# Test Plan
Generated by /plan-eng-review on 2026-04-24
Branch: dongxu-dev-0424
Repo: rl/gr00t-wholebodycontrol

## Affected Pages/Routes
- `G1 locked-waist planner selector seam` — verify early winner-evidence and
  bounded challenger fallback around
  `gr00t_manipulation/planner/curobo_grasp_planner.py` and
  `gr00t_manipulation/planner/waist_strategy.py`.
- `G1 representative visual harness` — rerun the 8-case suite under
  `tmp/phase67_2_harness/g1`.
- `Realman representative visual harness` — rerun the 8-case suite under
  `tmp/phase67_2_harness/realman`.

## Key Interactions to Verify
- `X35_Y0_Z15` and `X45_YN15_Z10` still pass, and the written summary records
  selected bottle yaw plus posture-quality metrics such as hug-center
  deviation and `holding_grip_center_error_mm` when emitted.
- The positive-Y G1 probe path and the full locked-waist goalset path use the
  same winner/challenger evidence surface.
- If a challenger family fails verify, the planner keeps the current
  execution-safe winner instead of switching to a regressive branch.
- Existing `g1_*`, `locked_waist_*`, and `approach_endpoint_*` metric keys
  remain stable for harness parsing.

## Edge Cases
- Positive-Y fast probe still uses the cheap goalset boundary and restores RNG
  state.
- Negative-Y cross-body probe still runs the full dry-run and still forwards
  probe-selected yaw / replan hint only for that historical scope.
- No selected winner or challenger metrics are emitted when goalset resolution
  is infeasible; failure stays explicit.
- Extra challenger verification does not exceed the bounded shortlist of the
  current winner plus one challenger.

## Critical Paths
- G1 planner unit coverage in
  `tests/grasp/planning/test_motion_gen_planner_unit.py` and
  `tests/grasp/planning/test_waist_strategy.py` for shared winner logic and
  fallback behavior.
- G1 stage/runtime coverage in
  `tests/grasp/planning/test_motion_gen_stage_pipeline.py` for late tie-break
  preservation and no regression in REIK hint forwarding.
- End-to-end no-regression gate: G1 representative
  `6 pass / 2 fail / 0 execution_error`, Realman representative
  `7 pass / 1 fail / 0 execution_error`, plus planning-time / REIK latency
  baselines.
