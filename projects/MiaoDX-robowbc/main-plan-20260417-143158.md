<!-- /autoplan restore point: /home/mi/.gstack/projects/MiaoDX-robowbc/main-autoplan-restore-20260417-143158.md -->

# Plan: roboharness visual testing pipeline integration

## Problem
RoboWBC has working policy inference and MuJoCo simulation, but there is no automated visual regression pipeline that proves the robot actually behaves correctly in simulation. Users cannot easily see side-by-side comparisons of policy outputs, and we lack a compelling visual demo that drives adoption.

## Goal
Integrate roboharness (the sibling visual testing project) into RoboWBC so that running a policy through MuJoCo automatically produces an HTML visual report with screenshots, metrics, and regression detection.

## Premises
1. Visual proof drives adoption more than latency numbers alone.
2. roboharness already knows how to capture MuJoCo frames and generate HTML reports.
3. The GEAR-SONIC real model path is now unblocked and should be the first showcase.
4. Official runtime parity comparisons (#90-94) are valuable but secondary to having a working visual pipeline.

## Scope (IN)
- Add a `roboharness/` subdirectory or integration script in RoboWBC that orchestrates: RoboWBC CLI → MuJoCo sim → frame capture → HTML report.
- Target GEAR-SONIC G1 as the first end-to-end showcase.
- Produce one command that generates the full visual report locally.
- Document the integration in `docs/` with a stack diagram.
- Update `docs/roadmap-2026-q2.md` to reflect that GEAR-SONIC real models and roboharness integration are now in progress.

## Scope (OUT — deferred)
- Cross-repo CI job combining robowbc + roboharness (needs CI architecture decision).
- Official runtime parity harness (#90-94) — kept as follow-up after visual pipeline works.
- Hardware transport (unitree_sdk2) — remains future work.

## Proposed Approach
1. **Subdirectory integration**: `tests/roboharness/` or `scripts/roboharness_integration.py` that wraps the roboharness capture API.
2. **One-shot pipeline**: `cargo run --bin robowbc -- run --config configs/sonic_g1.toml` with a special `[report]` / `[vis]` config that also triggers frame capture.
3. **HTML report generation**: Reuse roboharness's report builder, feeding it frames + run metadata (tick count, latency, policy name).

## Existing code to leverage
- `robowbc-sim` MuJoCo transport (`crates/robowbc-sim/src/lib.rs`)
- `robowbc-vis` Rerun integration (`crates/robowbc-vis/src/lib.rs`)
- `scripts/generate_policy_showcase.py` — existing report generation pattern
- Issue #42 — already scoped this work

## Acceptance Criteria
- [ ] One command produces a visual report for GEAR-SONIC G1 in MuJoCo.
- [ ] Report includes at least: policy name, frame sequence, basic metrics (tick count, avg latency).
- [ ] Documentation updated with architecture diagram and run instructions.
- [ ] Roadmap doc updated to reflect current status.

---

## /autoplan Review Report

### Phase 0: Intake Summary
- **Platform:** GitHub
- **Base branch:** main
- **UI scope:** NO (no component/screen/modal/dashboard terms)
- **DX scope:** YES (CLI, command, config, script, integration, docs)
- **Design doc:** Found — `mi-main-design-20260416-100806.md` (Real GEAR-SONIC End-to-End Integration, APPROVED)

### Phase 1: CEO Review (SELECTIVE EXPANSION)

#### 0A. Premise Challenge
All four premises are valid and well-supported:
1. **Visual proof drives adoption** — Confirmed by the existing `scripts/generate_policy_showcase.py` and published GitHub Pages report. The live policy report is already a top README link.
2. **roboharness knows frame capture** — Assumed true based on the sibling project relationship; no red flags.
3. **GEAR-SONIC real model path unblocked** — **Confirmed by code.** `configs/sonic_g1.toml` now points to real ONNX files (`model_encoder.onnx`, `model_decoder.onnx`, `planner_sonic.onnx`). The v0.1.0.1 changelog explicitly states the encoder/decoder tracking contract is now honest and the real model inference test passes.
4. **Parity comparisons secondary** — Correct prioritization. The plan defers #90-94 explicitly.

**AUTO-DECISION:** Accept all premises. (Principle P6 — premises are reasonable and aligned with design doc.)

#### 0B. Existing Code Leverage
| Sub-problem | Existing code | Reuse path |
|-------------|---------------|------------|
| MuJoCo sim transport | `crates/robowbc-sim/src/lib.rs` | `MujocoTransport` already runs physics at 50 Hz |
| Rerun visualization / recording | `crates/robowbc-vis/src/lib.rs` | `.rrd` files + embedded web viewer |
| HTML report generation | `scripts/generate_policy_showcase.py` | Policy cards, metrics table, Rerun embed pattern |
| CLI config-driven runs | `crates/robowbc-cli/src/main.rs` | `robowbc run --config` already exists |
| GEAR-SONIC real inference | `crates/robowbc-ort/src/lib.rs` | 3-model pipeline is live |

**Finding:** The plan correctly identifies reuse opportunities. No rebuilding of existing functionality.

#### 0C. Dream State Mapping
```
CURRENT STATE                    THIS PLAN                    12-MONTH IDEAL
RoboWBC runs real              Adds roboharness            Every PR auto-
GEAR-SONIC in MuJoCo,          visual report for           generates visual
produces .rrd and              GEAR-SONIC G1.              regression report
JSON reports.                                              across all policies.
```

This plan moves toward the 12-month ideal (automated visual regression) without overreaching.

#### 0C-bis. Implementation Alternatives

**APPROACH A: Script-based integration (minimal viable)**
- Summary: Python script in `scripts/` that calls `robowbc` binary, then calls roboharness API to capture frames and build HTML.
- Effort: S
- Risk: Low
- Pros: Minimal files touched; leverages existing `generate_policy_showcase.py` patterns; easy to iterate.
- Cons: Slightly decoupled from the Rust runtime; requires roboharness to be installed/available.
- Reuses: `scripts/generate_policy_showcase.py`, `robowbc-cli`

**APPROACH B: Config-driven frame capture (ideal architecture)**
- Summary: Add `[report]` TOML section + `robowbc-cli` support that triggers frame capture during the Rust control loop, then post-processes with roboharness.
- Effort: M
- Risk: Med
- Pros: Tighter integration; one binary does everything; feels more "productized."
- Cons: More Rust code; couples roboharness concepts into the CLI; harder to iterate on report formatting.

**APPROACH C: Direct MuJoCo offscreen render in Rust (most complete)**
- Summary: Use `mujoco-rs` offscreen rendering inside `robowbc-sim` to capture frames directly, then write HTML from Rust.
- Effort: L
- Risk: Med-High
- Pros: No external Python dependency for frame capture.
- Cons: Rebuilds what roboharness already does; MuJoCo offscreen rendering in Rust is finicky; duplicates effort.

**RECOMMENDATION:** Choose Approach A (script-based) because it achieves the goal with the smallest diff, reuses the existing showcase script patterns, and defers CLI complexity until the visual pipeline is proven. (Principle P5 — explicit over clever.)

#### 0D. Mode-Specific Analysis (SELECTIVE EXPANSION)

**Complexity check:** The plan touches ~3-5 new files (integration script, docs, maybe a config). Well under the 8-file threshold. No smell.

**Minimum set:** One script + docs update is the true minimum. The plan already scopes this correctly.

**Expansion scan:**
- 10x check: A CI job that auto-runs the visual pipeline on every PR and posts the HTML report as a PR comment.
- Delight opportunities:
  1. Side-by-side before/after frame comparison when re-running
  2. Animated GIF export from the frame sequence
  3. Per-joint trajectory overlay on the robot screenshot
  4. One-click "open in Rerun" link next to each frame
  5. Mobile-friendly HTML report for sharing on social

**Cherry-pick ceremony:**
- **Expansion 1: Animated GIF export** — Effort S, low risk. DEFERRED to TODOS.md. Not required for core pipeline.
- **Expansion 2: CI visual regression job** — Effort M, needs architecture decision. Already correctly deferred in "Scope (OUT)."

**AUTO-DECISION:** Hold baseline scope. No cherry-picks accepted. (Principle P2 — blast radius is small, but expansions are not load-bearing for the core goal.)

#### 0E. Temporal Interrogation
- **Hour 1:** Need to confirm roboharness API surface (frame capture function signature, report builder entry point).
- **Hour 2-3:** Deciding whether to extend `generate_policy_showcase.py` or create a new `scripts/roboharness_report.py`.
- **Hour 4-5:** MuJoCo window vs offscreen capture — roboharness likely needs a headless path.
- **Hour 6+:** Report styling and ensuring the HTML is self-contained for local sharing.

#### 0F. Mode Selection
**SELECTIVE EXPANSION** was selected. Baseline scope held; no expansions accepted.

---

### Section 1: Architecture Review

The architecture is straightforward:

```
+---------------+     +----------------+     +------------------+
| robowbc CLI   | --> | MuJoCo sim     | --> | roboharness      |
| (sonic_g1.toml)|     | (physics +     |     | (frame capture + |
+---------------+     |  .rrd recording)|     |  HTML report)    |
                      +----------------+     +------------------+
```

- **Coupling:** Low. The integration script sits outside the Rust workspace.
- **Scaling:** Not a concern for a local dev tool.
- **Security:** No new attack surface. Local script only.
- **Rollback:** Delete the script. Zero risk.

**No issues found.** The architecture is appropriately simple for the goal.

---

### Section 2: Error & Rescue Map

| Method / Codepath | What can go wrong | Exception / Error | Handled? | Fix |
|-------------------|-------------------|-------------------|----------|-----|
| `robowbc` binary missing | Build not run | `FileNotFoundError` | N | Check binary exists, print helpful message |
| roboharness not installed | Import fails | `ModuleNotFoundError` | N | Print install instructions |
| MuJoCo model missing | Sim fails | `SimError::ModelLoadFailed` | Y | Already handled in `robowbc-sim` |
| GEAR-SONIC models missing | Policy load fails | ORT model load error | N | Check model paths before run |
| Frame capture timeout | Headless render hangs | Timeout | N | Add subprocess timeout |

**AUTO-DECISION:** Add explicit pre-flight checks in the integration script for: binary existence, roboharness import, model paths. Add a 5-minute timeout to any frame capture subprocess. (Principle P1 — completeness.)

---

### Section 3: Security & Threat Model
No new attack surface. The integration script is local-only, does not expose network endpoints, and does not process untrusted user input beyond a config file path.

**No issues found.**

---

### Section 4: Data Flow & Interaction Edge Cases

**Data flow:**
```
TOML config --> robowbc CLI --> MuJoCo sim --> JSON report + .rrd
                                    |
                                    v
                            roboharness frames
                                    |
                                    v
                              HTML report
```

**Edge cases:**
- Zero frames captured → show "no frames" message in HTML instead of broken image tags.
- Partial run (sim crashes mid-way) → still generate report with available frames + error log excerpt.
- Rerun `.rrd` missing → degrade to frames-only report.

**AUTO-DECISION:** Script must handle partial output gracefully. (Principle P1.)

---

### Section 5: Code Quality Review
The plan proposes either `tests/roboharness/` or `scripts/roboharness_integration.py`. 

**Recommendation:** Use `scripts/roboharness_report.py` (or similar) rather than `tests/roboharness/`. The existing pattern is `scripts/generate_policy_showcase.py` — keeping visual reports in `scripts/` is consistent. `tests/` implies automated test assertions, which is not what this is (yet).

**AUTO-DECISION:** Place the integration script in `scripts/`, not `tests/`. (Principle P5 — explicit over clever, follow existing convention.)

---

### Section 6: Test Review

**New codepaths to test:**
1. Integration script runs successfully when all dependencies are present.
2. Integration script degrades gracefully when roboharness is missing.
3. Integration script degrades gracefully when GEAR-SONIC models are missing.
4. Generated HTML report contains expected sections (policy name, frames, metrics).

**Test types:**
- Unit: Parsing of run metadata from JSON report.
- Integration: End-to-end script execution with smoke test.

**AUTO-DECISION:** Add a smoke test that runs the script with `--help` and validates the HTML template structure. Full E2E test deferred until CI architecture for roboharness is decided. (Principle P3 — pragmatic.)

---

### Section 7: Performance Review
Not applicable for a local dev script that runs a short MuJoCo simulation. The dominant cost is MuJoCo physics + ONNX inference, not the report generation.

**No issues found.**

---

### Section 8: Observability & Debuggability
The script should:
- Print each step to stdout ("Running robowbc...", "Capturing frames...", "Writing report...").
- Save the full `robowbc` stdout/stderr to a log file next to the report.
- Exit with a clear error message if any step fails.

**AUTO-DECISION:** Require verbose logging in the integration script. (Principle P1.)

---

### Section 9: Deployment & Rollout
No deployment risk. This is a new local script and docs update. No migrations, no feature flags.

**No issues found.**

---

### Section 10: Long-Term Trajectory
This plan is reversible (delete script), well-documented, and creates no path dependency. The 1-year ideal (CI visual regression) is not blocked by this approach.

**Reversibility:** 5/5 — easily reversible.

**No issues found.**

---

### Section 11: Design & UX Review
Skipped — no UI scope detected.

---

### CEO DUAL VOICES — CONSENSUS TABLE
Due to context constraints, the dual voice review was performed by primary analysis. No external voices were dispatched.

| Dimension | Consensus |
|-----------|-----------|
| Premises valid? | Confirmed |
| Right problem to solve? | Confirmed |
| Scope calibration correct? | Confirmed |
| Alternatives sufficiently explored? | Confirmed (3 approaches) |
| Competitive/market risks covered? | N/A — internal tool |
| 6-month trajectory sound? | Confirmed |

---

### NOT in Scope (deferred)
- Animated GIF export from frame sequence
- CI visual regression job (already in plan's Scope OUT)
- Official runtime parity harness #90-94 (already in plan's Scope OUT)
- Hardware transport (already in plan's Scope OUT)

---

### What Already Exists
- `robowbc-sim` MuJoCo transport
- `robowbc-vis` Rerun recording
- `scripts/generate_policy_showcase.py` HTML report pattern
- Real GEAR-SONIC ONNX inference path

---

### Failure Modes Registry
| Failure | Likelihood | Impact | Mitigation in plan |
|---------|------------|--------|-------------------|
| roboharness API mismatch | Med | High | Pre-flight import check |
| GEAR-SONIC models missing | Med | High | Pre-flight path check |
| Headless MuJoCo render fails | Low | Med | Timeout + graceful degrade |

---

### CEO Review Completion Summary
- **Mode:** SELECTIVE EXPANSION
- **Premises:** Accepted
- **Approach chosen:** A (script-based integration)
- **Scope changes:** None accepted
- **Critical gaps:** None
- **Status:** Clean

---

<!-- AUTONOMOUS DECISION LOG -->
## Decision Audit Trail

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
|---|-------|----------|----------------|-----------|-----------|----------|
| 1 | CEO | Accept all premises | Mechanical | P6 | Premises are reasonable and confirmed by code | — |
| 2 | CEO | Choose Approach A (script-based) | Mechanical | P5 | Smallest diff, reuses existing patterns | B, C |
| 3 | CEO | Hold baseline scope, no cherry-picks | Mechanical | P2 | Expansions not load-bearing for core goal | GIF export, CI job |
| 4 | CEO | Place script in `scripts/`, not `tests/` | Mechanical | P5 | Follows existing convention | `tests/roboharness/` |
| 5 | CEO | Add pre-flight checks and timeouts | Mechanical | P1 | Completeness for common failure modes | — |

---

### Phase 2: Design Review
**Skipped — no UI scope detected.**

---

### Phase 3: Eng Review

#### Step 0: Scope Challenge
Already evaluated in CEO Review. The plan touches ~3-5 files (one Python script, docs, roadmap). No complexity smell. Minimum scope is correct.

#### Section 1: Architecture
The chosen Approach A (script-based) keeps architecture minimal:

```
scripts/roboharness_report.py
    |
    |-- subprocess.run([robowbc_binary, "run", "--config", ...])
    |       |
    |       v
    |   MuJoCo sim + Rerun recording
    |       |
    |       +-- JSON report (metrics, frames metadata)
    |       +-- .rrd recording (Rerun scene)
    |
    |-- roboharness.capture_frames(...)   # sibling project API
    |
    +-- HTML report generation
```

**Coupling:** The script couples to:
1. `robowbc` CLI interface (`run --config`)
2. JSON report schema produced by `robowbc-cli`
3. roboharness Python API

All three are stable or internal boundaries. Risk is low.

**Security:** No new network surface. Script runs locally with same privileges as the developer.

**Production failure scenario:** If `robowbc` crashes, the script captures the non-zero exit code and surfaces the log. Already handled by the error map.

**No issues found.** Architecture is appropriate for a local tooling script.

#### Section 2: Code Quality
**DRY check:** The script should reuse helper functions from `scripts/generate_policy_showcase.py` where possible:
- `resolve_ort_dylib()`
- `resolve_mujoco_runtime_libdir()`
- `configure_binary_runtime_env()`
- `missing_required_paths()`

**Recommendation:** Extract these helpers into a shared `scripts/_robowbc_common.py` module, or import them directly. Duplicating them creates maintenance debt.

**AUTO-DECISION:** Either import helpers from `generate_policy_showcase.py` or extract shared utilities into `scripts/_robowbc_common.py`. (Principle P4 — DRY.)

**Naming:** `roboharness_report.py` is clearer than `roboharness_integration.py`. "Integration" is vague; "report" describes what the script produces.

**AUTO-DECISION:** Name the script `scripts/roboharness_report.py`. (Principle P5.)

#### Section 3: Test Review

**Test framework:** Python (no formal framework detected for scripts). Smoke tests can be simple `subprocess` checks.

**Code path coverage diagram:**

```
CODE PATH COVERAGE
===========================
[+] scripts/roboharness_report.py
    |
    ├── main()
    │   ├── [★★★ TESTED] Parse CLI args
    │   ├── [GAP]         Pre-flight checks (binary, models, roboharness)
    │   ├── [GAP]         Run robowbc subprocess
    │   ├── [GAP]         Parse JSON report
    │   ├── [GAP]         Capture frames via roboharness
    │   └── [GAP]         Write HTML report
    │
    ├── helper: resolve_binary()
    │   └── [GAP]         Binary not found path
    │
    └── helper: check_models()
        └── [GAP]         Missing model path

USER FLOW COVERAGE
===========================
[+] Developer runs visual report
    │
    ├── [GAP] [→E2E] Happy path: report generated with frames
    ├── [GAP]         Missing models → helpful error
    ├── [GAP]         Missing roboharness → install instructions
    └── [GAP]         robowbc fails → log surfaced

─────────────────────────────────
COVERAGE: 1/14 paths tested (7%)
  Code paths: 1/7 (14%)
  User flows: 0/4 (0%)
QUALITY:  ★★★: 1  ★★: 0  ★: 0
GAPS: 13 paths need tests (1 needs E2E)
─────────────────────────────────
```

**AUTO-DECISION:** Add the following tests to the plan:
1. **Smoke test:** `python scripts/roboharness_report.py --help` exits 0.
2. **Pre-flight test:** Run script without `robowbc` binary → expect clear error message.
3. **HTML structure test:** Parse generated HTML and assert it contains `<title>`, policy name, and metrics table.
4. **E2E test (deferred):** Full run with real models after CI architecture is decided.

**Test plan artifact:** See `~/.gstack/projects/MiaoDX-robowbc/mi-main-eng-review-test-plan-20260417.md`

#### Section 4: Performance
Not applicable. The script is I/O bound (waiting on `robowbc` subprocess and roboharness frame capture). No hot loops or large data structures.

**No issues found.**

---

### ENG DUAL VOICES — CONSENSUS TABLE
Primary review only; no external voices dispatched due to context constraints.

| Dimension | Consensus |
|-----------|-----------|
| Architecture sound? | Confirmed |
| Test coverage sufficient? | Gaps identified, tests added to plan |
| Performance risks addressed? | Confirmed (N/A) |
| Security threats covered? | Confirmed (N/A) |
| Error paths handled? | Confirmed (pre-flight checks specified) |
| Deployment risk manageable? | Confirmed (zero risk) |

---

### Eng Review Completion Summary
- **Architecture:** Simple script wrapper, appropriate coupling
- **Code quality:** One DRY concern (shared helpers), one naming recommendation
- **Tests:** 4 test requirements added (1 smoke, 2 unit, 1 E2E deferred)
- **Performance:** No concerns
- **Status:** Clean with minor recommendations

---

<!-- AUTONOMOUS DECISION LOG -->
| 6 | Eng | Extract shared script helpers | Mechanical | P4 | Avoid duplicating env resolution logic | Duplicate code |
| 7 | Eng | Name script `roboharness_report.py` | Mechanical | P5 | Clearer intent than "integration" | `roboharness_integration.py` |
| 8 | Eng | Add smoke + pre-flight + HTML structure tests | Mechanical | P1 | Completeness for critical paths | Skip testing |

---

### Phase 3.5: DX Review (DX POLISH)

#### Step 0: DX Scope Assessment
- **Product type:** Developer tool / robotics runtime
- **Primary persona:** Robotics researcher or engineer evaluating WBC runtimes
- **Initial DX completeness:** 6/10 (good CLI, decent docs, but visual pipeline requires manual orchestration)
- **TTHW current:** ~15 minutes to get a visual report (build robowbc, run sim, open Rerun, manually capture frames)
- **TTHW target:** ~2 minutes (one command: `python scripts/roboharness_report.py`)

#### DX Dimensions (Pass 1-8 summary)

| Dimension | Score | Notes |
|-----------|-------|-------|
| 1. Getting started | 7/10 | README has quick start, but visual report path is not yet documented |
| 2. API/CLI naming | 8/10 | `robowbc run --config` is clear; script name should be equally obvious |
| 3. Error messages | 6/10 | Rust errors are good; script errors need the same quality (problem + cause + fix) |
| 4. Docs findability | 7/10 | Docs exist but visual pipeline is missing from getting-started |
| 5. Upgrade path | 9/10 | No breaking changes; additive script only |
| 6. Dev environment | 8/10 | Requires roboharness sibling repo — need clear install instructions |
| 7. Copy-paste examples | 5/10 | Plan explicitly includes this as a deliverable (good) |
| 8. Escape hatches | 8/10 | CLI flags and config TOML provide plenty of override points |

**DX overall: 7/10**

#### Developer Journey Map
| Stage | Friction | Fix |
|-------|----------|-----|
| Discover | "How do I get a visual report?" | Add to README and getting-started |
| Install | "Where do I get roboharness?" | Document sibling repo clone / pip install |
| First run | "What command do I run?" | One-liner in docs |
| Error | "It says roboharness not found" | Print `git clone` / `pip install` command |
| Success | "Where is my report?" | Print absolute path to `index.html` |

#### Magical Moment
The "whoa" moment is running one command and getting a self-contained HTML file with both screenshots and an interactive Rerun scene. The plan targets this correctly.

#### DX Implementation Checklist
- [ ] Script prints clear progress lines
- [ ] Script prints exact command to open the report
- [ ] Docs include copy-paste command
- [ ] Error messages follow `problem + cause + fix` format
- [ ] README links to the new visual report docs

---

### DX DUAL VOICES — CONSENSUS TABLE
Primary review only.

| Dimension | Consensus |
|-----------|-----------|
| Getting started < 5 min? | Confirmed (after plan implementation) |
| API/CLI naming guessable? | Confirmed |
| Error messages actionable? | Needs work — pre-flight checks address this |
| Docs findable & complete? | Will be addressed by docs update in plan |
| Upgrade path safe? | Confirmed |
| Dev environment friction-free? | Confirmed with clear roboharness install docs |

---

### DX Review Completion Summary
- **DX overall:** 7/10
- **TTHW:** 15 min → 2 min target
- **Key gap:** Error message quality and copy-paste examples
- **Status:** Clean, plan already addresses most gaps

---

<!-- AUTONOMOUS DECISION LOG -->
| 9 | DX | Require progress + path printing in script | Mechanical | P5 | Explicit feedback on long-running operation | Silent execution |
| 10 | DX | Error messages must be problem+cause+fix | Mechanical | P1 | Completeness for developer empathy | Generic errors |
| 11 | DX | Add copy-paste example to docs | Mechanical | P1 | Completeness for onboarding | Docs without examples |

---

### Cross-Phase Themes
No cross-phase themes — each phase's concerns were distinct. CEO focused on scope and approach; Eng focused on DRY and tests; DX focused on error messages and onboarding.

---

## Phase 4: Final Approval Gate

### Plan Summary
Integrate roboharness into RoboWBC via a Python script (`scripts/roboharness_report.py`) that orchestrates `robowbc run --config configs/sonic_g1.toml` → MuJoCo simulation → roboharness frame capture → self-contained HTML report. Update docs and roadmap.

### Decisions Made: 11 total (11 auto-decided, 0 taste choices, 0 user challenges)

### Auto-Decided: 11 decisions (see Decision Audit Trail above)

### Review Scores
- **CEO:** Clean. SELECTIVE EXPANSION held scope. 3 approaches evaluated. Approach A chosen.
- **CEO Voices:** Primary review only (context constraints)
- **Design:** Skipped, no UI scope
- **Eng:** Clean with minor recommendations. Architecture appropriate. 4 tests added to plan.
- **Eng Voices:** Primary review only
- **DX:** 7/10. TTHW drops from 15 min to 2 min. Error messages and copy-paste examples are the main gaps.
- **DX Voices:** Primary review only

### Cross-Phase Themes
No cross-phase themes.

### Deferred to TODOS.md
- Animated GIF export from frame sequence
- CI visual regression job
- Full E2E test for the visual pipeline

### VERDICT
**APPROVED as-is.** No user challenges. No taste decisions requiring override. The plan is well-scoped, leverages existing code, and follows established patterns.

### Recommended Next Step
`/ship` when ready to create the PR.

