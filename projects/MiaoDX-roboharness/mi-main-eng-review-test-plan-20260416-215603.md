# Test Plan
Generated by /plan-eng-review on 2026-04-16
Branch: main
Repo: MiaoDX/roboharness

## Affected Pages/Routes
- Showcase repo CI matrix — verify each showcase job runs independently and produces expected artifacts
- `examples/lerobot_g1_native.py` — verify it uses only public API before extraction
- `scripts/init-showcase-repo.py` — verify generator script does not create duplicate canonical sources

## Key Interactions to Verify
- Run each showcase `run.sh` and confirm exit 0 with valid `results.json` containing checkpoint data
- Install roboharness from `main` in a nightly CI job and confirm showcases still pass
- Verify `pip install -e /path/to/roboharness` works for local development against showcases

## Edge Cases
- Showcase with missing `run.sh` or non-executable permissions fails validation
- Malformed `harness_config.yaml` is caught before CI execution
- Dependency conflicts between two showcases (e.g., different torch versions) are isolated per venv
- External PR to showcase repo triggers maintainer approval before CI runs

## Critical Paths
- Extract `lerobot_g1_native.py` to standalone showcase, run it, and confirm it produces checkpoint captures using only public API
- Nightly CI against roboharness `main` catches API drift before PyPI release
