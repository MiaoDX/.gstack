# Test Plan
Generated by /ship on 2026-04-15
Branch: codex/design-review-mujoco-20260415-docs
Repo: MiaoDX/roboharness

## Affected Pages/Routes
- Generated MuJoCo report `report.html` — verify the shipped artifact still loads its embedded 3D scenes and remains readable on narrow viewports.
- Artifact Pack summary card — verify pass-state reports do not show failure-only root-cause or rerun metadata.

## Key Interactions to Verify
- Open the generated report and confirm each `Interactive 3D Scene` iframe points at the shipped Meshcat artifact, not a dead relative path.
- Resize the report to mobile-width and confirm the constraint table, artifact metadata table, and Meshcat viewer stay contained without sideways page scrolling.
- Inspect a PASS report and confirm the operator-facing summary says no rerun is required.

## Edge Cases
- Long code-like metadata values should wrap instead of forcing horizontal overflow.
- PASS manifests should emit empty evidence paths and avoid stale failure-only tokens.
- Meshcat iframe sizing should collapse cleanly below tablet widths.

## Critical Paths
- Generate a MuJoCo grasp report, open the HTML artifact, inspect summary proof, and trust the first viewport without dropping into broken embeds or contradictory metadata.
