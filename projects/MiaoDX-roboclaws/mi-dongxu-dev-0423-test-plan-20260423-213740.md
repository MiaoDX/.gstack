# Phase 05 Simplification — Test Plan Artifact
Branch: dongxu/dev-0423 | Generated by /autoplan Phase 3

## Baseline Gate (before any simplification)

```bash
pytest -x -q  # must pass before starting any plan
ruff check .
ruff format --check .
```

## Per-File Gates

### 05-04: transport.py
- `pytest tests/test_bridge.py tests/test_bridge_start_run.py -x -q`
- CRITICAL: `_reset_workspace_state` and `_handle_transport_exc` are declared DO-NOT-TOUCH zones
- Gap: ConnectError path and _SessionStoreReader.recover with corrupt fs are untested

### 05-06: vlm.py + kimi.py + openai.py
- `pytest tests/test_vlm.py tests/test_provider_retry.py tests/test_nvidia_provider.py -x -q`

### 05-07 T1: vision_bridge.py + bridge.py
- `pytest tests/test_openclaw_vision_bridge.py tests/test_bridge.py tests/test_bridge_start_run.py -x -q`
- NOTE: bridge.py re-exports from transport.py — verify all re-exported names still present after transport simplification

### 05-01: visualizer.py
- `pytest tests/test_visualizer.py tests/test_visualizer_soul_overlay.py tests/test_visualizer_structured.py -x -q`

### 05-07 T2: views.py + replay.py
- `pytest tests/test_views.py tests/test_replay.py -x -q`
- NOTE: views.py imports GameVisualizer from visualizer — must run after 05-01

### 05-03: reporter.py
- `pytest tests/test_reporter.py -x -q`

### 05-05: coverage.py + territory.py
- `pytest tests/test_coverage.py tests/test_territory.py tests/test_coverage_example.py tests/test_territory_example.py -x -q`
- Gap: Both files have _pos_to_cell impl — compare with common._pos_to_cell before removing either

### 05-02: mcp_server.py
- `pytest tests/test_openclaw_mcp_server.py -x -q`
- Gap: run_in_thread + done_event shutdown race untested
- NOTE: depends on views.py (05-07 T2) and vision_bridge.py (05-07 T1) — run after those

### 05-08: openclaw examples
- `pytest tests/test_openclaw_demo.py tests/test_openclaw_interactive.py tests/test_openclaw_nav_autonomous.py -x -q`

### 05-09: game examples
- `pytest tests/test_coverage_example.py tests/test_territory_example.py -x -q`

## Full Suite (after each commit group)
```bash
pytest -x -q
```

## Known Test Coverage Gaps
1. No E2E smoke: `bridge → mcp_server → views → visualizer` integration
2. transport.py: ConnectError path, _SessionStoreReader.recover with corrupt filesystem
3. mcp_server.py: run_in_thread() shutdown race after done()
4. visualizer.py: pixel-level rendering correctness (only shape/size tested)
5. games: examples tests only import-smoke, don't run the full game loop

## Recommended Execution Order (dependency-first)
1. transport.py (05-04)     — leaf
2. vlm.py + providers (05-06) — leaf
3. vision_bridge.py (05-07 T1 split) — leaf
4. visualizer.py (05-01)    — leaf
5. views.py (05-07 T2 split) — depends on visualizer
6. bridge.py (05-07 T1 remainder) — depends on transport
7. reporter.py (05-03)      — near-leaf
8. coverage.py + territory.py (05-05) — depends on common
9. replay.py (05-07 T2 remainder) — near-leaf
10. mcp_server.py (05-02)   — depends on views + vision_bridge
11. openclaw examples (05-08) — depends on bridge + mcp_server
12. game examples (05-09)   — depends on visualizer + game modules
